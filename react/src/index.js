import React from "react";
import ReactDOM from "react-dom";
import App from "./App";

/* ReactDOM.render(<App />, document.getElementById("root")); */

// redirected to App.js
// but you still need this existence of file to make the React work
